import { VariableType } from '../types/formula';

// Variable display labels
export const variableLabels: Record<VariableType, string> = {
  v1: 'Temperature',
  v2: 'Pressure',
  v3: 'Humidity',
  v4: 'Speed',
};
